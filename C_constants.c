#include <stdio.h>

int main(){
    // constant is a fixed value that cannot be altered by program during its execution

    const float PI = 3.141569;
    printf("I defined a constant float for Pi, where Pi = %.6f\n", PI);
    return 0;
}