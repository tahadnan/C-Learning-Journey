#include <ctype.h>
#include <stdio.h>
#include <string.h>

// UNCOMPLETED TRAINING


int main(){
    char first_name[] = "Taha";
    char middle_name[] = "Yasser";
    char last_name[] = "Adnan";
    char full_name[100] = "";

    printf("Lowercase %s using the strlwr() func => %s", first_name, tolower(first_name));
}